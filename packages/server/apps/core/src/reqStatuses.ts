import { ReturnStatusType } from './types';

const SUCCESS: ReturnStatusType = { status: 'success' };
const FAILED: ReturnStatusType = { status: 'failed' };

export const Status = { SUCCESS, FAILED };
